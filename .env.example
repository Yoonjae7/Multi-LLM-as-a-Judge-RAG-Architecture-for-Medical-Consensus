# MediRAG-West live backend. Both keys are free, no card required.
#
# Groq  (specialist agents)  -> https://console.groq.com/keys
# Gemini (SafeJudge judges)  -> https://aistudio.google.com/apikey
#
# Copy this file to .env.local for local `vercel dev`, or add both
# variables under Project Settings > Environment Variables on Vercel.
# Never commit the real values.

GROQ_API_KEY=
GEMINI_API_KEY=
